# SOMNIUM — session boot file

**Read `BRIEF.md` first. The GOAL at its top wins every conflict.**

- **What:** 2.5D painted co-op journey — two knights wake in a dark-fantasy-paperback
  world (Frazetta/McBride/Nasmith + film grain, one accent hue per scene).
- **Port:** 5131 (`npm run dev`, strictPort — if taken, it's already running; reuse it).
- **Stack:** Vite + TS + raw Canvas 2D. Own git repo.
- **Status:** **Gate 0 = atmosphere-benchmark loop. Iteration 3 (vs Ian's board)
  DONE 2026-07-12 — awaiting Ian's benchmark verdict.** All five gap-analysis items
  worked (cloud drama, field density, haze separation + tower, thorn sentinel,
  near-frame soft focus — see `docs/ART_DIRECTION.md` + iteration log in
  `docs/GATE0_REPORT.md`). Proof: `docs/gate0/gate0.mp4` + stills. 165fps headed.
- **Next step:** Ian grades vs his benchmark board (watch the mp4 or `npm run dev`
  → :5131; R replays the wake; the hero still is `docs/gate0/12-native-1080p.png`).
  PASS → Gate 0 closes → Phase 1 journey spine per BRIEF.md §7.9.
  KEEP-ITERATING → work his new notes into the Red-Sun Field only; no content.
- **Laws in force:** animation-first gate · no cyan holo/scanlines/microtype · real
  clicks only in QA · visible-page canvas capture · no shadowBlur per-entity ·
  title + one tag max of UI text.
